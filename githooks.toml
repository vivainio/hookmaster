pre-commit = "python tasks.py formatcheck"
pre-push = "pytest"

# [forbidden-strings]
# Checked in staged files during pre-commit.
# Key = literal string, Value = glob pattern(s) ("*" for all files)
# "<<<<<<< " = "*"
# "console.log" = ["*.py", "*.ts"]
# "binding.pry" = "*.rb"

# [ascii-only]
# Reject non-ASCII characters (emojis, smart quotes, invisible unicode) in staged files.
# files = ["*.toml", "*.yml", "*.sh", "*.py"]
# exclude = ["tests/*"]
# allow = ["→", "—"]
